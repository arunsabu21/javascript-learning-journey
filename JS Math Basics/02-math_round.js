// Math.round()
// Round the number to the nearest whole number.
console.log(Math.round(4.7));  // 5   (because 0.7 ≥ 0.5)
console.log(Math.round(4.4));  // 4   (because 0.4 < 0.5)
console.log(Math.round(9.5));  // 10  (0.5 rounds up)
console.log(Math.round(-2.3)); // -2
console.log(Math.round(-2.7)); // -3
