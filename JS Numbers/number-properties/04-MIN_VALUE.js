// JavaScript MIN_VALUE
// Number.MIN_VALUE is a constant representing the lowest possible number in JavaScript.
let x = Number.MIN_VALUE;
console.log(x);

// The smallest number possible in JavaScript is:

// 5e-324

/*
MAX_VALUE → biggest number JS can store

MIN_VALUE → smallest positive number JS can store

MIN_VALUE is close to 0, but not 0

Anything smaller becomes 0
*/